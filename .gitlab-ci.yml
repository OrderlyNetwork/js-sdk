include:
  - project: "orderlynetwork/orderly-fe/common-ci"
    ref: main
    file: "momorepo-release.yaml"
  # - project: "orderlynetwork/orderly-security/orderly-security-templates"
  #   ref: main
  #   file: "npm-security-baseline.yml"

stages:
  - scan
  - check_tag
  - build_docker
  - deploy
  - post-build-audit
  - release

# variables:
#   IMAGE_NAME: "storybook"
#   APP_IMAGE_TAG: "asia-northeast1-docker.pkg.dev/woo-orderly/orderly-network/${IMAGE_NAME}:${$CI_COMMIT_TAG}"
#   ENABLE_PUBLISH: "false"

# workflow:
#   rules:
#     # - if: '($CI_COMMIT_BRANCH =~ /^internal\// || $CI_COMMIT_BRANCH =~ /^pre-release/) && $CI_COMMIT_MESSAGE =~ /publish/'
#     - if: $CI_COMMIT_BRANCH == "cicd-erik"
#       variables:
#         ENABLE_PUBLISH: "true"
#     - when: always

generate-sbom:
  stage: scan
  image:
    # name: asia-northeast1-docker.pkg.dev/woo-orderly/orderly-security/anchore/syft:v1.43.0
    name: asia-northeast1-docker.pkg.dev/woo-orderly/orderly-security/anchore/syft:v1.43.0-debug
    # name: asia-northeast1-docker.pkg.dev/woo-orderly/orderly-devops/curl:jq
    # name: anchore/syft:v1.43.0-debug
    # entrypoint: [""]
  script:
    - syft . -o cyclonedx-json=bom.json
    # - echo "Hello World"
  artifacts:
    paths: [bom.json]
  tags:
    - dev
    - build