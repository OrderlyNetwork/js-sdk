import { Tabs, Tab } from "nextra/components";

# Orderly CLI reference

## Install

```sh npm2yarn
npm i -g @orderly/cli
```

## Usage

If you installed Prisma as a development dependency, you need to prefix the prisma command with your package runner.

## Commands

### `create`

The `create` command

###### options

The `create` command recognizes the following options to modify its behavior:

###### Examples
