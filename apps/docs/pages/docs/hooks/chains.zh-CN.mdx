import { CodeLive } from "@/components/doc/preview/codeLive";
import orderbookRow from "@/example/chains.example";

# Chains

`@orderly.network/hooks` 用来查询 Orderly 支持的链。

- `useChains`

## 如何获取 Chains 列表

```typescript
import { useChains } from "@orderly.network/hooks";

const [chains, { findByChainId }] = useChains();
```

`findByChainId`函数可以传链的 ID 来获取某一个链的配置信息。

```typescript /findByChainId/
import { useChains } from "@orderly.network/hooks";

const [_, { findByChainId }] = useChains();

const chain = findByChainId(421613);
```

## 示例

<CodeLive code={orderbookRow} disabled />

{/* <CodePreview /> */}
