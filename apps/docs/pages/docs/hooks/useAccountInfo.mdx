# useAccountInfo

`useAccountInfo()`

获取当前登录的用户的账户信息

## import

```ts copy
import { useAccountInfo } from "@orderly/hooks";
```

## Usage

```ts copy
// get account info
const { data, loading, error } = useAccountInfo();
```

**Parameters**

- `key` string

**Returns**
