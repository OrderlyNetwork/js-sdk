# positions

包含跟仓位相关的计算公式.

## import

```ts copy
import { positions } from "@orderly/futures";
```

## Methods

### notional

`notional(qty: number, mark_price: number): number`

单个仓位价值

**Parameters**

- `qty`

Type: `number`

- `mark_price`

Type: `number`

### unrealizedPnL

`unrealizedPnL(inputs: UnrealPnLInputs): number`

单个仓位未实现盈亏

**Parameters**

inputs

Type: `UnrealPnLInputs`

```typescript
type UnrealPnLInputs = {
  markPrice: number; // symbol mark price
  openPrice: number; // 开仓价格
  qty: number; // 仓位数量
};
```

**Returns**

Type: `number`
