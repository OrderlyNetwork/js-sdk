# util

## Overview

DOM helper for iframe content: waitForElm(iframeDocument, selector) returns a Promise that resolves when an element matching the selector appears in the document (uses MutationObserver if not immediately found).
