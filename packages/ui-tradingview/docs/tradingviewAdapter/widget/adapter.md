# adapter

## Overview

Legacy settings adapter: defaultSettings (chart.lastUsedTimeBasedResolution, trading.chart.proterty, hint.startFocusedZoom), loadAdapterSettings(adapterKey), persistAdapterSettings(adapterKey, key, value), getSettingAdapter(adapterKey) returning { initialSettings, setValue, removeValue } for localStorage-backed persistence.
