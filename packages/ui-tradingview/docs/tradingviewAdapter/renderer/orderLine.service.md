# OrderLineService

## Overview

Draws order lines (limit, stop, etc.) on the chart. renderPendingOrders(pendingOrders) and updatePositions(positions) keep lines in sync; removeAll() clears order shapes.
