# PositionLineService

## Overview

Draws position lines on the TradingView chart (e.g. position price level). Uses chart instance and broker; renderPositions(positions) and removePositions() manage lifecycle.
