# helpers

## Overview

Shared types and helpers for datafeed: RequestParams (record of param name to string | string[] | number), UdfResponse (s: string), UdfOkResponse, UdfErrorResponse, logMessage(message) (guarded by isLoggingEnabled), getErrorMessage(error).
