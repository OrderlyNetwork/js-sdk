# autoHideText.tsx

## Overview

Component that shows text with optional auto-hide or overflow behavior (e.g. truncate or collapse). Used for referral codes or long labels.

## Props

| Name | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| (component-specific) | | | | See source for full props |

## Usage Example

```tsx
import { AutoHideText } from "../components/autoHideText";
<AutoHideText text={referralCode} />
```
