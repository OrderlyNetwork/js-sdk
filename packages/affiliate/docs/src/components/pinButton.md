# pinButton.tsx

## Overview

Button component that displays a pin icon. Used for pinning or fixing referral codes or items in the UI.

## Props

| Name | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| (component-specific) | | | | See source for full props |

## Usage Example

```tsx
import { PinButton } from "../components/pinButton";
<PinButton onClick={handlePin} />
```
