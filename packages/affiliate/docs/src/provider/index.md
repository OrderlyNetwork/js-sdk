# provider

## Overview

Referral context and provider. Re-exports everything from `context.ts` and `provider.tsx`: `ReferralContext`, `useReferralContext`, `ReferralProvider`, and types (`ReferralContextProps`, `ReferralContextReturns`, `TabTypes`, `UserVolumeType`, `ChartConfig`, `Overwrite`, `BuildNode`).

## Files

| File | Language | Description |
|------|----------|-------------|
| [context.ts](context.md) | TypeScript | ReferralContext, useReferralContext, types and enums |
| [provider.tsx](provider.md) | React | ReferralProvider component |
| [index.ts](context.md#exports) | Re-exports | Re-exports context and provider |
