# icons/traderIcon.tsx

## Responsibility of TraderIcon

Trader role icon. Used in tab or role selection (trader vs affiliate).

## TraderIcon Props

IconProps (size, className, SVG props).

## TraderIcon Example

```tsx
<TraderIcon size={18} />
```
