export enum StatisticsTimeRange {
  "1d" = "1d",
  "7d" = "7d",
  "30d" = "30d",
  "All" = "all_time",
}

export enum ReferralCodeFormType {
  Create = "create",
  Edit = "edit",
  Reset = "reset",
}

export enum ReferralCodeFormField {
  ReferralCode = "referralCode",
  RebateRate = "rebateRate",
}
