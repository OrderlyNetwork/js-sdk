# searchInput

Search input bound to markets context. Uses `useMarketsContext()` for `searchValue`, `onSearchValueChange`, `clearSearchValue`. Supports composition events (IME). Props: `classNames`, `suffix` (from `InputProps`). Renders `Input` with search icon and clear button.
