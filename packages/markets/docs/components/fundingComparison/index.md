# fundingComparison

Funding comparison across exchanges. Exports: `FundingComparison`, `useFundingComparisonScript`, `FundingComparisonWidget`, `MobileFundingComparison`. Uses `useFundingColumns` (columns.tsx) and `useEXchanges` for exchange list.
