# ChartBase

## Overview

Minimal wrapper component that renders only `ChartRenderer` with no context or size. Used as a simple base when full Chart (with ResizeObserver and ChartContext) is not needed.

## Props

None.

## Usage example

```tsx
import { ChartBase } from "./chartBase";

<ChartBase />
```
