import React, { FC } from "react";
import { Box, Flex, Text } from "@orderly.network/ui";

type SingleSwapProps = {};
export const SingleSwap: FC<SingleSwapProps> = (props) => {
  return <Box></Box>;
};
