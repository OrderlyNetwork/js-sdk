# rwaStatus

## Overview

RWA status tag component used in positions table (e.g. symbol column). Displays RWA-related status for the given symbol.

## Usage example

```tsx
import { RwaStatusTag } from "./rwaStatus";
<RwaStatusTag symbol={symbol} />
```
