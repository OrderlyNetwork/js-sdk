# positionHistory.ui

## Overview

Presentational components for position history: desktop table (`PositionHistory`) and mobile list (`MobilePositionHistory`). Use data and handlers from `usePositionHistoryScript`.
