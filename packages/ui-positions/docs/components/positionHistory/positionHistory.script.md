# positionHistory.script

## Overview

Hook that fetches position history (closed positions) with date filter and optional sort persistence. Returns dataSource, pagination, loading, and sort state for desktop/mobile UI.
