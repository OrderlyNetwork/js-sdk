# usePositionHistoryColumn

## Overview

Returns desktop table column config for position history (e.g. symbol, close time, avg open, close price, PnL, ROI, share). Used by `PositionHistory` DataTable.
