# reversePosition.script

## Overview

Hook for reverse position: `useReversePositionScript` returns display info, validation error, `reversePosition()` submit, and `isReversing` state. Used by ReversePositionWidget dialog.
