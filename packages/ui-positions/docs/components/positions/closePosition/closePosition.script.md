# closePosition.script

## Overview

Hook and types for close position flow: `useClosePositionScript` returns state for desktop/mobile close UI (e.g. type). Most state lives in `PositionsRowProvider` / `usePositionsRowContext`.
