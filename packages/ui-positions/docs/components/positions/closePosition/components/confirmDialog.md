# confirmDialog

## Overview

Market close confirmation dialog (`MarketCloseConfirm`). Registered with `registerSimpleDialog(MarketCloseConfirmID, MarketCloseConfirm, { size: "md", closable: false })` from package index.
