# useSubAccountQuery

## Overview

Hook to fetch data for multiple sub-accounts (e.g. `GET /v1/client/info` with `accountId` list). Used by combine positions to load account info for MMR/IMR and grouping.
