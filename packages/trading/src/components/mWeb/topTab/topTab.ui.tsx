import { FC } from "react";
import { Flex, Text } from "@orderly.network/ui";
import { TopTabState } from "./topTab.script";


export const TopTab: FC<TopTabState> = (props) => {

    return (
        <Flex></Flex>
    );
}
