import { FC } from "react";
import { Flex, Text } from "@orderly.network/ui";
import { MarginRatioState } from "./marginRatio.script";


export const MarginRatio: FC<MarginRatioState> = (props) => {

    return (
        <Flex></Flex>
    );
}
