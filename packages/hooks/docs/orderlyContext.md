# orderlyContext

## Overview

Orderly config context and provider: `OrderlyProvider`, `OrderlyConfigContextState`, `FilteredChains`, and related types. Provides config state to the tree. See source: `src/orderlyContext.ts`.
