# useOrderEntry

## Overview

Next-generation order entry hook. Returns `OrderEntryReturn` with state and methods for placing and managing orders. See source: `src/next/useOrderEntry/useOrderEntry.ts`.
