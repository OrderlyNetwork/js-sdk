# scaffold.ui

> Location: `packages/ui-scaffold/src/components/scaffold/scaffold.ui.tsx`

## Overview

Desktop layout: renders top navbar (with announcement), body (optional left sidebar + content area), and footer. Consumes refs and heights from `useScaffoldScript` and scaffold props for custom slots and class names.
