# sum.test.ts

## Overview

Unit tests for the `sum` function from `./sum`. Verifies that `sum(1, 2)` equals `3`.

## Usage example

Tests are run via the project test runner (e.g. `pnpm test` or `vitest`). No direct usage in application code.
