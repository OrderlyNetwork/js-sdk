# errors

## Overview

API error type used by the fetch layer when the server returns an error response (e.g. 400 with message and code).

## Files

| File | Description |
| ---- | ----------- |
| [apiError.md](./apiError.md) | `ApiError` class extending `Error` with `code` |
