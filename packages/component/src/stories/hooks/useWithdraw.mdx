import { Meta, Canvas } from "@storybook/blocks";
import * as WithdrawStories from "./useWithdraw.stories";

<Meta of={WithdrawStories} />

# useWithdraw

## Import

```bash
import { useWithdraw } from "@orderly.network/hooks";
```

## Usage

```tsx
import { useWithdraw } from "@orderly.network/hooks";

const SimpleComponent = () => {
  //...
  const { withdraw, isLoading, maxAmount, availableBalance, unsettledPnL } =
    useWithdraw();
  //...
};
```

<Canvas of={WithdrawStories.Default} />
