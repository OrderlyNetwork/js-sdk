import { Meta, Canvas } from "@storybook/blocks";
import * as DepositStories from "./useDeposit.stories";

<Meta of={DepositStories} />

# useDeposit

## Import

```bash
import { useDeposit } from "@orderly.network/hooks";
```

## Usage

```tsx
import { useDeposit } from "@orderly.network/hooks";

const SimpleComponent = () => {
  //...
  const { balance, allowance, approve, deposit } = useDeposit();
  //...
};
```

<Canvas of={DepositStories.Default} />
