# usePrivateQuery

A React hook for querying the Orderly GraphQL API with a private key.

## Import

```ts
import { usePrivateQuery } from "@orderly-network/hooks";
```

## Usage

```ts
const { data, loading, error } = usePrivateQuery(query, variables, options);
```
