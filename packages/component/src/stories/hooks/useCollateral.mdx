import { Meta, Canvas } from "@storybook/blocks";
import * as CollateralHookStories from "./useCollateral.stories";

<Meta of={CollateralHookStories} />

# useCollateral

## Usage

```tsx
import { useCollateral } from "@orderly/hooks";

const SimpleComponent = () => {
  //...
  const { totalCollateral, freeCollateral } = useCollateral();
  //...
};
```

<Canvas of={CollateralHookStories.Default} sourceState="shown" />
