import { Meta, Canvas } from "@storybook/blocks";
import * as PositionHookStories from "./usePosition.stories";

<Meta of={PositionHookStories} />

# usePositionStream

## Usage

```tsx
import { usePositionStream } from "@orderly.network/hooks";

const SimpleComponent = () => {
  //...
  const [{ totalCollateral, freeCollateral }] = usePosition();
  //...
};
```

<Canvas of={PositionHookStories.Default} />
