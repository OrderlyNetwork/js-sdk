import { Meta } from "@storybook/blocks";

<Meta title="hook/useQuery" />

# useQuery

```shell
yarn add @orderly/hooks
```

## Usage

```tsx
import { useQuery } from "@orderly/hooks";

import { type API } from "@orderly/types";

export const useMarketInfo = (symbol: string) => {
  return useQuery<API.MarketInfo>(`/public/futures/${symbol}`);
};
```

## APIs
