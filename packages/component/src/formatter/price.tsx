import { FC } from "react";

interface FormattedPriceProps {
  value: number;
  currency?: string;
  as?: string;
}

export const FormattedPrice: FC<FormattedPriceProps> = () => {
  return <div></div>;
};
