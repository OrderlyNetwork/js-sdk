# depositForm.script.tsx

> Location: `packages/ui-transfer/src/components/depositForm/depositForm.script.tsx`

## Overview

`useDepositFormScript(options)` — hook that holds deposit form state and logic. Options typically include chain, token, amount, and callbacks. Returns props for `DepositForm` UI.
