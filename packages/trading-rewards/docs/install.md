# install

## Overview

Placeholder for installing the Trading Rewards layout as a UI extension. The implementation is fully commented out; it would register a layout extension for `ExtensionPositionEnum.TradingRewardsLayout` with `TradingRewardsLayout` and a layout builder.

## Exports

None (file is commented out; would export nothing when uncommented).

## Usage

Currently unused. Uncomment and wire `layoutBuilder` from layout if extension-based registration is needed.
