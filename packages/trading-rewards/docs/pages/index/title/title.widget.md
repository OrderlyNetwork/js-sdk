# title.widget

## Overview

Widget that connects `useTitleScript` to `Title`. No props.

## Exports

### TitleWidget (component)

Stateless; uses hook and passes result to `Title`.

## Usage example

```tsx
<TitleWidget />
```
