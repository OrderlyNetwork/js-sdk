# rewardsHistory.widget

## Overview

Widget that connects `useRewardsHistoryScript` to `RewardHistory`. No props.

## Exports

### RewardsHistoryWidget (component)

Stateless; uses hook and passes result to UI.

## Usage example

```tsx
<RewardsHistoryWidget />
```
