# curEpoch.widget

## Overview

Widget that connects `useCurEpochScript` to `CurEpoch` UI. No props.

## Exports

### CurEpochWidget (component)

Stateless; uses hook and passes result to `CurEpoch`.

## Usage example

```tsx
<CurEpochWidget />
```
