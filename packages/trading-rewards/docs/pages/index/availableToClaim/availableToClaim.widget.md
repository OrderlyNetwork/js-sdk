# availableToClaim.widget

## Overview

Widget that connects `useAvailableScript` to `AvailableToClaim`. No props.

## Exports

### AvailableToClaimWidget (component)

Stateless; uses hook and passes result to UI.

## Usage example

```tsx
<AvailableToClaimWidget />
```
