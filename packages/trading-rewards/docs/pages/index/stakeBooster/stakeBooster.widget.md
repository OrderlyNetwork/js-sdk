# stakeBooster.widget

## Overview

Widget that connects `useStakeBoosterScript` to `StakeBooster`. No props.

## Exports

### StakeBoosterWidget (component)

Stateless; uses hook and passes result to UI.

## Usage example

```tsx
<StakeBoosterWidget />
```
